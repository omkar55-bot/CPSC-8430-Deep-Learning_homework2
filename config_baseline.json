{
  "description": "Baseline configuration to reach BLEU@1 = 0.6 as shown in training tips",
  
  "baseline_info": {
    "target_bleu1": 0.6,
    "training_epochs": 200,
    "lstm_dimension": 256,
    "learning_rate": 0.001,
    "optimizer": "Adam",
    "training_time_expected": "72 minutes on GTX 960",
    "vocab_min_count": 3
  },
  
  "data_parameters": {
    "data_root": "E:/imgsynth/MLDS_hw2_1_data",
    "max_frames": 80,
    "max_caption_length": 20,
    "vocab_min_count": 3,
    "analyze_dataset": true
  },
  
  "model_parameters": {
    "model_type": "attention",
    "video_feature_dim": 4096,
    "hidden_dim": 256,
    "embedding_dim": 256,
    "num_layers": 2,
    "dropout": 0.3,
    "attention_dim": 256
  },
  
  "training_parameters": {
    "batch_size": 32,
    "num_epochs": 200,
    "learning_rate": 0.001,
    "weight_decay": 0.0,
    "grad_clip": 5.0,
    "num_workers": 4,
    "use_scheduler": false
  },
  
  "advanced_features": {
    "use_scheduled_sampling": true,
    "initial_sampling_prob": 0.0,
    "final_sampling_prob": 0.25,
    "sampling_schedule_epochs": 100,
    "exposure_bias_solution": "scheduled_sampling"
  },
  
  "beam_search": {
    "beam_size": 5,
    "length_penalty": 1.0,
    "use_for_evaluation": true
  },
  
  "evaluation": {
    "eval_every": 10,
    "eval_samples": 200,
    "target_metrics": {
      "BLEU-1": 0.60,
      "BLEU-4": 0.25,
      "METEOR": 0.28,
      "CIDEr": 0.45
    }
  },
  
  "logging": {
    "save_dir": "./checkpoints_baseline",
    "save_every": 10,
    "keep_checkpoints": 10,
    "log_attention_weights": true
  },
  
  "hardware_optimization": {
    "pin_memory": true,
    "non_blocking": true,
    "mixed_precision": false
  }
}